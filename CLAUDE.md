# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Domain Skills

Core portfolio management domain knowledge (always load; always available):
@./.claude/skills/portfolio-manager/SKILL.md

Load these skills when implementing backtesting or strategy evaluation logic:
- **`./.claude/skills/backtest-expert/SKILL.md`**

## .NET solution / build
- Solution is `.slnx` (not `.sln`); use `dotnet sln <file>.slnx` / VS 2022 17.x+.
- Central Package Management is in effect: `Directory.Packages.props` (versions) + `Directory.Build.props` (shared properties: TargetFramework, Nullable, ImplicitUsings, RestorePackagesWithLockFile, and `IsTestProject`/`IsPackable` auto-set for any project named `*.Tests`).
- After changing `Directory.Packages.props` or any `.csproj`, regenerate lock files with `dotnet restore --force-evaluate`; verify with `dotnet restore --locked-mode`.
- Before assuming a `PackageReference` is redundant because a `ProjectReference`d project already has it, verify empirically (`dotnet restore` + `dotnet list package --include-transitive`) rather than reasoning from the reference graph alone — analyzer/source-generator packages (e.g. Riok.Mapperly) and anything under `PrivateAssets=all` never flow transitively, even without explicit blocking.
- Code style: explicit constructors, not C# primary constructors (verified: 13 vs 0 across the codebase) — don't apply IDE "use primary constructor" suggestions.

## Docker Compose / Visual Studio Container Tools
- `docker-compose.dcproj` (`Microsoft.Docker.Sdk`) can be added to `TraderEngine.slnx` safely — `dotnet build`/`test`/`restore` silently skip it (SDK only resolves inside VS), so it won't break CI or CLI-driven builds.
- VS Fast Mode debugging only builds a Dockerfile's *first* stage, then bind-mounts the host build output over it — a single-stage Dockerfile that `COPY`s a prebuilt `bin/Release/.../publish` folder will NOT work for one-click F5 (needs a manual `dotnet publish` first). Use a self-contained multi-stage Dockerfile (base/build/publish/final, SDK builds from source) for real one-click debugging; keep the prod/CI Dockerfile separate/untouched if CI depends on its current shape.
- The "host build output" Fast Mode bind-mounts into `/app` is the *entire live project source folder* (e.g. `D:\...\TraderEngine.Web` → `/app`), not a separately-produced `bin/Debug` copy — confirmed via `docker inspect <container> --format "{{json .Mounts}}"` while an F5 session was running. VS's Container Tools MSBuild targets (`Microsoft.VisualStudio.Azure.Containers.Tools.Targets`) also add their own dedicated `wwwroot` → `/app/wwwroot` mount automatically per web-project service, on top of that. This means edits to `wwwroot` (including compiled output from `pnpm run watch`) are visible inside a running Fast Mode container immediately, with zero extra `docker-compose.debug.yml` config needed — ASP.NET Core's `ReloadStaticAssetsAtRuntime` (default-enabled for non-published/dev "build manifest" runs, which Fast Mode always is) then re-serves the changed file with a recomputed ETag on the very next request, no rebuild or restart required (verified directly via native `dotnet run`: editing `wwwroot/js/site.js` while the process was running changed the served ETag/content-length/body on the next request). Don't manually add a redundant wwwroot bind mount to "fix" this — check `docker inspect --format "{{json .Mounts}}"` first, VS likely already did it. Also don't try to hand-simulate Fast Mode with a plain `bin/Debug` folder mount instead of the whole project source — that crashes the app outright at startup (`ArgumentException: The path must be absolute`), because a Windows-built static web assets manifest bakes in Windows absolute host paths (e.g. `D:\...\wwwroot\`) that are meaningless inside a Linux container; VS avoids this by mounting the live source tree itself rather than a copied build output.
- `--env-file` is a root-level Compose CLI flag only (`docker compose --env-file X up`, not `up --env-file X`) — can't be smuggled in via `DockerComposeUpArguments`/`DockerComposeBuildArguments` (those append after the subcommand).
- `DockerComposeEnvFilePath` (singular, documented) takes exactly one file. `DockerComposeEnvFilePaths` (plural, semicolon-delimited) is real and shipped but undocumented on Microsoft Learn — confirmed by decompiling `...MSBuild\Sdks\Microsoft.Docker.Sdk\tools\Microsoft.Docker*.dll` (`strings` + grep) and validating live via `MSBuild.exe docker-compose.dcproj -t:DockerGetComposeEnvFilePaths -getTargetResult:DockerGetComposeEnvFilePaths`. Use it to layer `.env.example;.env` (later file wins) instead of pre-merging files.
- A service's `env_file:` YAML key injects raw vars into the *container* and never participates in `${VAR}` interpolation when Compose parses the YAML — the two mechanisms don't interact, don't conflate them when `environment:` blocks rename/compose vars (e.g. `CMC_API_KEY` → `CoinMarketCap__API_KEY`).
- MSBuild.exe is at `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\amd64\MSBuild.exe`; in Git Bash use `-t:Target` not `/t:Target` (the `/` gets mangled into a path).
- VS live-syncs `.slnx`/`.dcproj`/`Directory.Packages.props`/`.csproj` while open in the IDE — expect concurrent external edits mid-session; re-read a file before editing it rather than assuming your last-known copy is current.
- Compose merges (unions) a service's `profiles` list across `-f` files by default — `profiles: []` in an overlay does NOT clear a base file's `profiles: [foo]`, it stays `[foo]`. Force a real override with the `!reset` YAML merge tag (`profiles: !reset []`). Verified via `docker compose config --services`.
- `composeLaunchUrl`'s `{ServiceIPAddress}`/`{ServicePort}` tokens require VS to resolve and directly reach the container's internal Docker-network IP — a separate, less reliable path than the host-published port already confirmed working (`docker-compose.yml`'s `traderengine.web` maps `8081:80`). When the browser silently never launches on F5 despite debugging working fine (raising `launchBrowserTimeout` doesn't help either), that token resolution/reachability is the likely failure point. Even a partial fix (`"{Scheme}://localhost:8081"`, token + literal host mixed) fails outright with "Container Tools" Output pane error `The URL '...' is not valid.` — mixing tokens and literals isn't tolerated. Use a fully literal URL instead (`"http://localhost:8081"`, no tokens at all); this app has no SSL configured anywhere so hardcoding the scheme is safe. Keep the literal port in sync with `docker-compose.yml`'s `ports:` mapping if it ever changes.
- `launchSettings.json`'s `launchBrowserTimeout` (Docker Compose only, seconds) defaults far too low for this app — Microsoft's own docs example uses `1`. `traderengine.web` runs an EF Core migration (`Database.MigrateAsync`) plus admin-seed logic before Kestrel starts listening, which blows past that window; VS then silently skips the browser launch (no error) rather than failing loudly. Set it explicitly (e.g. `60`) in the `"Docker Compose"` launch profile. Plain JSON, not JSONC — no comments allowed in this file.
- VS's "Docker Compose" F5 debugging does not pass `--profile <name>` even when `launchSettings.json`'s `composeProfile.includes` names one, AND expects a running container for *every* service defined across whatever files it loads (base `docker-compose.yml` + `AdditionalComposeFilePaths`), regardless of that service's `profiles:` gate — a profile-gated service in those files is silently skipped by `docker compose up`, then VS fails with "Can not find the container for the service '\<name\>'." Confirmed by reproducing manually (`docker compose up` with no `--profile` flag skips the same services). Two different fixes depending on intent: if the service *should* always run under F5, clear its `profiles` in the VS-only debug overlay with the `!reset` tag (see above); if it must stay opt-in-only (e.g. a one-shot job container), keep it out of every file the dcproj loads entirely — put it in its own compose file, referenced only manually via `-f`, never added to `AdditionalComposeFilePaths` or the base file. The `launchSettings.json` `composeProfile.includes` key was removed as dead/misleading rather than trusted, once this was understood.

## Web frontend theming (TraderEngine.Web)
- Dark/light mode follows Bootstrap 5.3's official color-mode pattern (`data-bs-theme` on `<html>`, driven by `TraderEngine.Web/src/js/theme.ts` — localStorage-backed light/dark/auto, cycled via a single button rather than Bootstrap's docs dropdown). Compiled to `wwwroot/js/theme.js` and loaded as a plain non-deferred `<script src>` at the very top of `<head>` (not bundled into `site.js`, which loads deferred at the end of `<body>`) — it must run synchronously before first paint or the FOUC-avoidance is defeated.
- `site.scss`'s Bootstrap variable overrides ($primary, $body-bg, etc.) must be set *between* the `functions` and `variables`/`variables-dark` imports, not after — Bootstrap's official documented order. Setting them after silently breaks anything derived from those variables at import time (e.g. `$theme-colors` → the `--bs-primary` custom property, `--bs-secondary`, etc.), since Sass locks in whatever value existed when each `_variables.scss` map was built.
- The navbar logo (`_Layout.cshtml`) renders `~/images/favicon.svg` via `<object>`, not `<img>`. `favicon.svg` has its own light/dark `<g id="light-icon"/"dark-icon">` groups gated by an internal `@media (prefers-color-scheme)` rule, which only ever sees the OS-level preference — not this app's resolved theme, which can diverge from it via the manual toggle. `<object>` gives same-origin JS access to `.contentDocument`, letting `theme.ts`'s `syncBrandLogo()` force-override those groups' inline `style.display` (inline style beats the internal stylesheet regardless of the media query) so the logo tracks the actual applied theme. An `<img>`-loaded SVG is sandboxed to its own browsing context with no such access — this is why `<object>` was chosen over the simpler tag despite the extra complexity.
- `<object>` creates a nested browsing context, so it's subject to framing policy — `Program.cs`'s global security-header middleware (`X-Frame-Options`/CSP `frame-ancestors`) applies to *every* response including `favicon.svg` itself, and `DENY`/`'none'` blocks all framing, even same-origin, so the navbar logo's own `<object>` embed got blocked by its own server headers. Fixed by relaxing both to `SAMEORIGIN`/`'self'` — still fully blocks the actual threat (third-party clickjacking framing), just permits the app framing its own resources.
- `asp-append-version` has no built-in tag helper support for `<object data>` (only `img`/`script`/`link` ship one) — `TraderEngine.Web/TagHelpers/ObjectTagHelper.cs` adds it, modeled directly on the framework's own `ImageTagHelper` (same `IFileVersionProvider`/`UrlResolutionTagHelper` pattern, same `Order = -1000`, same "append version to the still-`~/`-prefixed path, then resolve the tilde" sequencing). Auto-discovered via the existing `@addTagHelper *, TraderEngine.Web` in `_ViewImports.cshtml` — no extra registration needed.

## Bitvavo integration: known future improvements (not yet implemented)
- The limit-order-with-market-fallback feature (`RebalancingService.PlaceLimitThenFallback`) cancels a timed-out limit order and places a separate market order for the remainder, surfaced as two `OrderDto` entries linked by `OrderDto.IsSuperseded`. Bitvavo's `PUT /order` can instead reprice an existing limit order in place (same order id) rather than cancel+repost; a price update that crosses the spread matches immediately against resting orders under standard price-time-priority matching, the same fill behavior as posting a fresh aggressive limit order, since `orderType` itself cannot be changed via update per Bitvavo's docs. Switching to this would collapse the fallback to a single order (no more `IsSuperseded` split) and save a round trip, but it touches the already-tested fallback logic and its own verification pass, deliberately deferred rather than bundled into the rate-limit/WebSocket-reliability work.
- `BitvavoWebSocketClient` only subscribes to the private `account` channel. Bitvavo's `ticker/book` REST endpoint (used today by `BitvavoExchange.GetBestBidAsk` for limit-order pricing) only returns top-of-book best bid/ask, not depth; if a limit order's size is large relative to top-of-book liquidity, a size-aware price (e.g. walking the public `book` endpoint or a live `book`/`bookUpdate` WS subscription to compute a depth-weighted price) would likely fill more reliably than naively quoting at the naked best bid/ask. Worth revisiting if the limit-order feature's fallback-to-market rate turns out to be high in practice.
- `BitvavoWebSocketClient.Dispatch` only fails the pending auth `TaskCompletionSource` on a WS-level `"error"` event; an error arriving after auth succeeds (e.g. a malformed subscribe, an account-level API error surfaced over the socket) is logged but has no path to reach whichever in-flight `WaitForOrderEndedAsync` call is waiting on that connection, so that call just rides out its full timeout before falling back to REST instead of failing fast. The REST fallback still makes this correct today, just slower than it could be; routing WS-level errors to in-flight waiters (keyed by market or a correlation id) would close the gap.
- Bitvavo charges an order's fee in quote currency **in addition to** the trade value (not netted from the received base asset), confirmed via `docs.bitvavo.com/docs/rest-api/create-order`'s example (`feeCurrency: "EUR"` for an EUR-quoted market). `MockExchange.NewOrder`'s test-only fee model diverges from this (nets the fee out of the received asset instead, per its own `TODO` comment); don't assert exact balance-exhaustion behavior against `curBalance`'s post-trade EUR remainder in tests, assert against the requested `order.AmountQuote` instead.
- `PlaceLimitThenFallback`'s dust-remainder branch (`RebalancingService.cs`, ~line 275) only has an escape hatch for `OrderSide.Sell`: a sell remainder under `MinOrderSizeInQuote` (€5 on Bitvavo) still gets fully liquidated via an `Amount`-based order, since Bitvavo allows fully closing a position under the quote-value minimum. A `Buy` remainder under that same €5 floor has no equivalent path and is simply dropped, silently, with no log line. Confirmed live: a €20 limit buy for XRP filled about €15 before timing out, left a roughly €5 remainder, and that remainder was abandoned outright instead of getting a market-order attempt, not "dust" in the negligible sense, a full 25% of the intended order gone because the leftover happened to land at or under the exchange minimum.
  - **Observability, fixed**: this branch, `BuyUnderagesAndVerify`'s pre-scaling `AmountQuote < MinOrderSizeInQuote` filter, and its post-claim filter all now log via `LogInformation` when they skip/drop an order.
  - **The balance-race blocker described here is now largely resolved**: buy legs no longer size against a static snapshot; each concurrently-dispatched leg atomically claims its share from `BudgetLedger` (see below), clamped to whatever's actually left, including that leg's own reserved fee, immediately before placing. What's still missing: that ledger isn't threaded through into `PlaceLimitThenFallback` (called per-order, no access to the batch's shared budget), so rounding a buy-side dust remainder up to the exchange minimum here is still deferred, but the "real plumbing" blocker is now much smaller, since the live-budget mechanism already exists and mainly needs plumbing through, not designing from scratch.
- **Every Bitvavo market is quoted against EUR only** (no direct crypto-to-crypto pairs, e.g. no ETH-BTC market); every rebalance leg round-trips through EUR as the sole tradable quote currency. This is an exchange-imposed constraint, not a design choice in this codebase. If a future exchange integration supported direct crypto-to-crypto pairs, swapping directly between two held assets (skipping the EUR hop) could in principle cut the fee/latency cost of a rebalance roughly in half for that leg. Caveat this would need to be verified against, not assumed: direct altcoin-to-altcoin pairs typically carry far lower volume/liquidity than pairs quoted against a major fiat or stablecoin, so the wider spread/slippage on a thin direct pair could easily offset or exceed the fee savings from cutting out the EUR middle-hop, especially for smaller-cap alts. Not worth pursuing until an exchange with such pairs is actually integrated, and even then only after checking real liquidity on the specific pairs involved.

## Portfolio management model: no risk-profile concept, EUR is the risk lever
- This app has no investor risk-profile concept (Conservative/Moderate/Growth/Aggressive or similar) anywhere in the codebase, no per-position thesis tracking, and no tax-lot/cost-basis logic, confirmed via repo-wide search, zero hits for any of these. It's a single, global, systematic market-cap-index rebalancer: `MarketCapService.BalancedTargetAllocs` computes target weights as `(weighting × marketCap)^(1/NthRoot)` normalized by the sum, with no hard per-asset or per-tier concentration ceiling enforced anywhere (`NthRoot` only *dampens* the power-law curve, it doesn't cap any single position).
- `ConfigReqDto.QuoteAllocation`/`QuoteTakeout` (EUR) is the actual risk-off lever here, not a crypto stablecoin. `TagsToIgnore` defaulting to `["stablecoin"]` is unrelated to risk sizing: it just keeps crypto-stablecoins out of the ranked/traded universe (correct, since Bitvavo only trades against EUR anyway, so a crypto stablecoin holding isn't independently useful the way EUR itself is). Don't conflate the two: excluding stablecoin-tagged assets from ranking is not the same lever as `QuoteAllocation`, which is the one that actually controls risk-off sizing.

## Rebalancing algorithm: interleaved sell/buy execution (`RebalancingService.cs`, `BudgetLedger.cs`)
- Sells and buys within one `Rebalance` run execute **concurrently** against a shared `BudgetLedger` (`TraderEngine.Common/Services/BudgetLedger.cs`), not sequentially (`await` all sells, then `await` all buys) as originally implemented. The old sequential design gated the entire buy phase on `Task.WhenAll` of every sell leg, including the single slowest one (up to the full ~60s limit-order fill-wait plus market fallback), even buys whose funding came from a different, already-completed sell.
- `BudgetLedger.Deposit()` is fed by each sell leg's own settled `AmountQuoteFilled - FeePaid`, authoritative for that leg (exchange-reported), not an estimate; Bitvavo's fee-in-addition-to-trade-value semantics (above) makes this net-proceeds figure exact, sidestepping the old design's `TakerFee` settlement-lag buffer entirely for the sell side (that buffer still applies to the ledger's initial pre-sell balance seed, which retains ordinary staleness risk).
- **No upfront fairness estimate.** Every buy leg calls `ClaimAsync` for its own full, unscaled target; there is no "guess a ratio from projected sell proceeds and pre-scale every request by it" step. A ratio-estimate design was tried first and reverted after a real production bug: a manual rebalance left one asset about €5 short of target and about €5 of EUR unspent, self-corrected only by a second manual run, root-caused to the order-list `Rebalance` overload's funding estimate being blind to `Amount`-only dust/full-liquidation sells (not rare, any asset dropped from the target list liquidates this way), so the estimate undershot reality and permanently dropped a leg the account could have easily afforded. An initial fix added a retry pass after reconciliation; a deeper rework then removed the estimate (and the retry pass it required) entirely, since a fair ratio can be computed directly, from true numbers, at the one point they're actually known.
- **All-or-nothing batch resolution, not per-claim.** A claim registered via `ClaimAsync` is not auto-resolved just because it individually fits within `_available` right now, since resolving whichever claims happen to fit, one at a time, would let a small request "cut in line" ahead of a much larger one purely by size, consuming funds a proportional split would have partly reserved for the larger claim too. `Deposit()`/`Settle()` auto-check whether the pool now covers the sum of everything currently pending, resolving all of it together if so; a caller registering a whole batch via a loop of `ClaimAsync` calls must call `TryResolvePendingBatch()` once, explicitly, after the whole batch is registered (`ExecuteInterleaved` does this right after materializing `buyTasks`), since checking after each individual registration would suffer the exact same cut-in-line problem for the first-registered claim.
- `BudgetLedger.Complete(actualAvailable)` performs a **best-effort reconciliation** once every sell has resolved: one more `GetBalance` fetch, applied as a correction that can move the ledger's total either up or down (catching anything the deposit-sum tracking couldn't model, e.g. other account activity, a missed event). `actualAvailable` first has `_inFlight` (money already claimed by a still-in-progress, not-yet-`Settle`d buy leg) subtracted from it, since a claim not yet round-tripped through the exchange isn't reflected as held in a fresh balance fetch; skipping that adjustment would let the same funds be handed out twice. A reconciliation failure is logged and swallowed, not fatal, since every deposit already fed the ledger directly, so this fetch is a safety net on top of that, not the only source of truth. Whatever's still pending at that point gets a **fair, proportional split** of whatever's genuinely left, computed once from the true final numbers, never a guess. Two easy-to-miss precision traps here: (1) snapshot the pool into a local before the split loop and read from the snapshot, not the mutating `_available` field, or every claim after the first sees an already-decremented pool and is unfairly short-changed; (2) compute each share as `claim.Requested * pool / totalOutstanding` (multiply before dividing), not `claim.Requested * (pool / totalOutstanding)` (a pre-computed ratio), since the latter can materialize a non-terminating decimal and land a hair short of an exact value even when the true answer is exact, e.g. a single pending claim's fair share should equal the pool exactly.
- **Ordering matters in `ExecuteInterleaved`**: `buyTasks` must be fully materialized (registering every claim) before `reconcileTask` is constructed. `BudgetLedger.ClaimAsync` is a plain synchronous method, not `async`, so registration completes before that call's first real `await` regardless of how slow the exchange is, meaning this ordering is a genuine structural guarantee, not one that merely happens to hold for synchronous test doubles. Get this backwards and, for a fully synchronous exchange with no sells to wait for, `ReconcileLedgerAfterSells` would run straight through to `Complete()` before any buy leg had registered, firing against an empty pending queue and resolving every claim individually via `ClaimAsync`'s post-`Complete()` branch instead of the fair, proportional split `Complete()` performs across whatever's actually pending, collapsing fairness into first-claimed-wins. Caught by test regressions during implementation, not by design review, across several iterations of this feature; a subtle enough failure mode to be worth re-checking if this method is ever restructured.
- `InternalsVisibleTo("TraderEngine.Common.Tests")` (`TraderEngine.Common/AssemblyInfo.cs`) exists specifically so `BudgetLedger`, deliberately `internal` and not part of the public `IRebalancingService` surface, can get direct, isolated concurrency stress testing (`BudgetLedgerTests.cs`) rather than only indirect coverage through `RebalancingService`'s own (entirely synchronous-test-double-based) test suite, which cannot exercise real parallel claim/deposit races.

## Bitvavo integration: confirmed API quirks (fixed 2026-08-20)
- `DELETE /order` (single-order cancel) requires `operatorId` in the query string, just like `POST /order` and the bulk `DELETE /orders` already did; omitting it fails outright with `errorCode 203` ("operatorId parameter is required"). `BitvavoExchange.CancelOrder` didn't send it, so every single-order cancel silently failed: a timed-out limit order's held balance was never released, causing the next order (e.g. `PlaceLimitThenFallback`'s market fallback) to fail with `errorCode 216` (insufficient balance) for money that looked free but wasn't. Don't assume `operatorId` is optional on any Bitvavo endpoint just because one sibling endpoint's docs/example omits it.
- Bitvavo markets enforce a per-market `minOrderInBaseAsset` floor in addition to the account-wide `minOrderInQuoteAsset` (`MinOrderSizeInQuote`); an order can clear the flat €5 quote-value check yet still be rejected (`errorCode 212`) if the base-asset quantity itself is below that specific market's floor. Already mapped into `MarketDataDto.MinOrderSizeInBase` via `ApiMapper`, just never consulted; now checked in `RebalancingService.PlaceLimitThenFallback` right after a limit order's `Amount` is finalized (the only point the exact base-asset quantity is known).
- `BitvavoExchange.GetMarket` calls the per-market `GET /markets?market=X` endpoint (not the unfiltered bulk `GET /markets`) and caches each market's result individually via a DI-registered `IMemoryCache` (10s TTL), keyed by a private `MarketDataCacheKey` record so it can't collide with any other feature sharing that singleton cache instance. A bulk-fetch-behind-one-cache-entry design was tried first and reverted: a market absent from an unfiltered bulk response is ambiguous (genuinely delisted vs. a transient omission), whereas the single-market endpoint's `errorCode 205` is unambiguous for the exact market asked about. `IMemoryCache` is registered via `builder.Services.AddMemoryCache()` in `Program.cs` as a singleton, so cached market data is shared across concurrent rebalance runs/users too, not just within one `BitvavoExchange` instance (which is DI-registered Scoped). This still collapses `RebalancingService.GetTopRankingAllocs`'s sequential per-market status-check loop from N calls down to however many distinct markets are involved (each fetched once, reused for the rest of the TTL), without `RebalancingService`, `IExchange`, or any test double needing to know caching is happening. The 10s TTL is a deliberate freshness/efficiency trade-off, not arbitrary: long enough to cover that one sequential status-check burst, short enough to have reliably expired by the time `PlaceLimitThenFallback` checks a BUY leg's minimum base-asset size, since sells are placed and fully verified (up to a 60s fill-wait each) before any buy leg is sized, so a stale `MarketStatus` (e.g. a market halted mid-run) never gates a real trading decision for long. `BitvavoExchange.GetAsset` uses the exact same pattern (own `AssetDataCacheKey`, same shared `_publicDataCache` field/TTL), since it's the same shape of data: public and account-agnostic. "Public, account-agnostic data" is the general eligibility bar for adding a new endpoint to this shared cache; anything account-specific (balances, orders) must never go in it. Neither `GetMarket` nor `GetAsset` has stampede/single-flight protection (a plain `IMemoryCache.TryGetValue`/`Set`, no lock): two concurrent misses for the same key can both hit the API before either populates the cache. Accepted deliberately, since per-call cost is cheap and cross-caller overlap on the same symbol at the same instant is rare; revisit only if usage patterns change.

## Logging
- EF Core's `Microsoft.EntityFrameworkCore.Database.Command` category logs every executed SQL statement at `Information` level by default, drowning out real signal in container logs. Set to `Warning` explicitly in `appsettings.json`'s `Logging:LogLevel` (done in `TraderEngine.API` and `TraderEngine.Web`); migrations/errors still log since those aren't gated by this category.

## PowerShell 5.1 (this machine's default)
- `Set-Content -Encoding utf8NoBOM` doesn't exist (PS 6+ only). Use `[System.IO.File]::WriteAllLines($path, $lines, (New-Object System.Text.UTF8Encoding $false))` instead.
- `Get-Content` without `-Encoding UTF8` reads via the system codepage, mangling non-ASCII characters (e.g. em dashes become `â€”`) — always pass `-Encoding UTF8` explicitly when round-tripping repo files.
