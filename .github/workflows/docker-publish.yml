name: Docker publish

on:
  schedule:
    - cron: '45 0 * * *'
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

permissions:
  contents: write
  packages: read
  actions: read
  security-events: write

jobs:

  analysis:
    uses: bvandevliet/Actions.Shared/.github/workflows/dotnet-analysis.yml@master

  gitversion:
    needs: [analysis]
    uses: bvandevliet/Actions.Shared/.github/workflows/git-version.yml@master

  build-push:
    needs: [analysis,gitversion]
    uses: bvandevliet/Actions.Shared/.github/workflows/docker-build-push.yml@master
    strategy:
      matrix:
        project:
          - TraderEngine.API
          - TraderEngine.CLI
    with:
      PROJECT: ${{ matrix.project }}
      majorMinorPatch: ${{ needs.gitversion.outputs.majorMinorPatch }}
      preReleaseLabel: ${{ needs.gitversion.outputs.majorMinorPatch }}
      semVer: ${{ needs.gitversion.outputs.majorMinorPatch }}
      assemblySemVer: ${{ needs.gitversion.outputs.majorMinorPatch }}
      assemblySemFileVer: ${{ needs.gitversion.outputs.majorMinorPatch }}
      CR_REGISTRY: ${{ vars.CR_REGISTRY }}
    secrets:
      CR_USERNAME: ${{ secrets.CR_USERNAME }}
      CR_PASSWORD: ${{ secrets.CR_PASSWORD }}